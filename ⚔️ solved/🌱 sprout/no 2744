#include <iostream>

using namespace std;

int main() {

	string a; cin >> a;
	for (int i = 0; i < a.size(); i++) {
		a[i] = ('A' <= a[i] && a[i] <= 'Z') ? a[i] += 32 : a[i] -= 32;
	}
	for (int i = 0; i < a.size(); i++) {
		cout << a[i];
	}
	return 0;
}
